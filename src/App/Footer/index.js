// @flow
import * as React from 'react';
import {
  Box,
  Typography,
} from '@mui/material';

const Footer = (): React.Node => (
  <Box>
    <Typography>Hello... my name is Angelica</Typography>
  </Box>
);

export default Footer;
